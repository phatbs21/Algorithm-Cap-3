#include <bits/stdc++.h>
using namespace std;
int main()
{
#ifndef ONLINE_JUDGE
    freopen("canhgacbaotang.inp", "r", stdin);
    freopen("canhgacbaotang.out", "w", stdout);
#endif
    int n, m;
    cin >> n >> m;
    vector<int> s(m + 1), t(m + 1), c(m + 1);
    for (int i = 1; i <= m; i++)
        cin >> s[i] >> t[i] >> c[i];
    
}